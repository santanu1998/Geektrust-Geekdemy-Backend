package constants

import (
	"testing"
)

func Test_constants(t *testing.T) {
	// There is literally nothing to test in constants module
	// But gotestsum requires test to be present for each of the module.
	// So, here we go
	if true == true {
		t.Logf("Test successful")
	}
}
